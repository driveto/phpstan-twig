{% for number in numbers %}
    {% for letter in letters %}
        {{ foo.getIntValue(number) }}
        {{ foo.getStringValue(letter) }}
    {% endfor %}
    {{ foo.getIntValue(number) }}
{% endfor %}
